package org.p365.util;

import org.im4java.core.ConvertCmd;
import org.im4java.core.IMOperation;

public class ImgConvertWithIm4 {
	IMOperation opstdImOperation = new IMOperation();  
    
	//opstdImOperation.addImage();
    //op.addImage();  
    //op.resize(rectw, recth, '^').gravity("center").extent(rectw, recth);  
    //op.addImage();  

    ConvertCmd convert = new ConvertCmd(true);  
    //convert.createScript("e:\\test\\myscript.sh",op);  
    //convert.run(op, srcPath, desPath);  
}
